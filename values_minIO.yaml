mode: standalone

rootUser: "admin"
rootPassword: "pizzachèvremiel2026"

persistence:
  enabled: true
  size: 10Gi

resources:
  requests:
    memory: 512Mi
    cpu: 250m

service:
  type: ClusterIP

consoleService:
  type: ClusterIP
  